__default__:
    queue : "short"
    time : 60
    n : 1
    mem : 2000
    name : "{rule}.{wildcards}"
    log : "logs/cluster/{rule}.{wildcards}.log"
    err : "logs/cluster/{rule}.{wildcards}.err"

make_stranded_bedgraph:
    time: 15
    mem: 4000

bedgraph_to_bigwig:
    time: 10
    mem: 2000
